import React from 'react';
import { StyledFooter } from './styles';

export const Footer: React.FC = () => {
	return (
		<StyledFooter>
			<img src='koala-logo.png' height='35' alt='Company logo' />
		</StyledFooter>
	);
};
